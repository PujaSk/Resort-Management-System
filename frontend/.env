### Then create the `.env` file in `frontend` folder:
#In VS Code, right-click on the `frontend` folder → **New File** → name it `.env` → paste:

VITE_API_URL=https://resort-management-system.onrender.com/api
